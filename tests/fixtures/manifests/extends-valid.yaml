# Fixture: manifest using `profile: minimal` + extends with add/remove.
# Expected: validate-manifest.py exits 0.
schema_version: 1
profile: minimal
extends:
  add_rules: []          # empty add lists are fine
  remove_hooks: [doctor-on-session-start]   # removing existing component is OK
