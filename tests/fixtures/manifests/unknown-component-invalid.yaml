# Fixture: manifest references a component that does not exist with the given kind.
# Expected: validate-manifest.py exits 1 with error naming the unknown reference.
schema_version: 1
rules:
  - phantom-rule
  - no-console-log    # this one is valid; only phantom-rule should fail
